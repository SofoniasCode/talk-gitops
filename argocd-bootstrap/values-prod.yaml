environment: prod
targetNamespace: talk-prod

# `gitopsRepo` (Org/repo) is set per-tenant via `helm --set` (Terraform does
# this automatically in talk-infra). See values-dev.yaml for details.
gitopsRepo: ""
gitopsBranch: main
