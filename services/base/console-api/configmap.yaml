apiVersion: v1
kind: ConfigMap
metadata:
  name: console-api-config
  labels:
    app.kubernetes.io/name: console-api
data:
  APP_ENV: production
  AUTHZ_BASE_URL: http://authz:8000
  DEPENDENCY_TIMEOUT_SECONDS: "5"
