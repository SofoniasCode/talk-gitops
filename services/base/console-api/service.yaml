apiVersion: v1
kind: Service
metadata:
  name: console-api
  labels:
    app.kubernetes.io/name: console-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: console-api
  ports:
    - name: http
      port: 8000
      targetPort: http
