apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: talk-gateway-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: REPLACE_WITH_GITOPS_REPO_URL
    targetRevision: main
    path: gateway/overlays/azure-prod
  destination:
    server: https://kubernetes.default.svc
    namespace: talk-prod
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
