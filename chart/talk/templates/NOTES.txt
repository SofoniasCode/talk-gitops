Talk has been deployed to namespace {{ .Release.Namespace }}.

  Cloud:         {{ .Values.cloud }}
  Environment:   {{ .Values.global.environment }}
  Base domain:   {{ default .Values.global.domain .Values.global.baseDomain }}
  TLS:           {{ .Values.tls.enabled }}{{ if .Values.tls.wildcard.enabled }} (wildcard {{ .Values.tls.wildcard.secretName }}){{ end }}
  external-dns:  {{ .Values.externalDns.enabled }}
  Registry:      {{ .Values.registry.server }}

Per-product subdomains:
  Admin:    {{ if .Values.tls.enabled }}https{{ else }}http{{ end }}://{{ include "talk.host" (list $ "admin") }}/
  Agents:   {{ if .Values.tls.enabled }}https{{ else }}http{{ end }}://{{ include "talk.host" (list $ "agents") }}/
  Console:  {{ if .Values.tls.enabled }}https{{ else }}http{{ end }}://{{ include "talk.host" (list $ "console") }}/  (staff only)
  Zitadel:  {{ if .Values.tls.enabled }}https{{ else }}http{{ end }}://{{ include "talk.host" (list $ "zitadel") }}/

To check pod status:

  kubectl get pods -n {{ .Release.Namespace }}
  kubectl get certificate -n {{ .Release.Namespace }}
{{ if .Values.externalDns.enabled }}
To check DNS sync:

  kubectl logs -n external-dns deploy/external-dns --tail=50
{{ end }}
